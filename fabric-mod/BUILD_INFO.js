// SpriteHack build.bat - Alternative: calls gradlew.bat which downloads Gradle + builds the mod
// Usage: Double-click build.bat in fabric-mod folder

/**
 * BUILD SCRIPT - Downloads and runs Gradle to compile SpriteHack as a real Fabric mod
 *
 * Requirements:
 *   - Internet connection (first run only - downloads Gradle 8.5 ~110MB + MC mappings)
 *   - Java 17 (auto-detected from MCreator installation)
 *
 * Output: fabric-mod/build/libs/spritehack-3.5.0.jar
 *         Copy this to %APPDATA%/.minecraft/mods/
 */
